# Habr Tech Radar — copy to .env and adjust (optional for stub pipeline)
# Prefix: HTR_

# Logging: DEBUG, INFO, WARNING, ERROR
HTR_LOG_LEVEL=INFO

# When true, pipeline avoids side effects where applicable (reserved for future use)
HTR_DRY_RUN=true

# Set to 1 to ingest one demo article and run the full stub pipeline (no network)
HTR_DEMO_MODE=false

# RSS feeds (comma or whitespace separated). Empty = no HTTP requests. Default: Habr ru articles feed.
# HTR_HABR_RSS_URLS=https://habr.com/ru/rss/articles/

# Seen article IDs (JSON). Created on first successful save.
# HTR_STATE_FILE=.habr_tech_radar_seen.json

# Per-feed HTTP timeout (seconds)
# HTR_RSS_FETCH_TIMEOUT_SECONDS=30

# Future integrations (leave empty for local stub runs)
HTR_TELEGRAM_BOT_TOKEN=
HTR_TELEGRAM_CHAT_ID=
HTR_OPENAI_API_KEY=
